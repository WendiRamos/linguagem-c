#include <stdio.h>

int main(void){
    int numero , quadrado;
    float fracao;
    char letra;

    // printf("Entre com um caractere:");
    // scanf("%c", &letra);

    // printf("o caractere digitado foi %c", letra);

    // printf("Entre com um numero inteiro:");
    // scanf("%d", &numero);

    // printf("Entre com um numero fracionario:");
    // scanf("%f", &fracao);

    // quadrado = numero * numero;

    // printf("O quadrado e %d por %f e igual a %f.", quadrado, fracao, quadrado/fracao);
    
    if(numero % 2 == 0){
    if(!(numero % 2)){
    printf("O numero %d e par. \n", numero);
    }
    else{
        printf("O numero %d e impar. \n", numero);
    }
    }


    return 0;
}
